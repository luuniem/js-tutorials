let isAccountLocked = true;
let userRole = "admin";
console.log(isAccountLocked === "admin" ? "locked" : "welcome");

let temp = 100;

console.log(
  temp > 80
    ? "weather is too hot"
    : temp <= 50
    ? "weather is too cold"
    : "weather is perfect"
);
